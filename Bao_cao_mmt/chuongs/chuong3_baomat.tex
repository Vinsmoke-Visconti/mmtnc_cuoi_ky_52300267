\section{TRIỂN KHAI BẢO MẬT ĐA LỚP}

\subsection{Nguyên tắc thiết kế Tường lửa}
Hệ thống áp dụng chính sách bảo mật Zero-Trust tại vùng biên. Chính sách mặc định (Default Policy) của chuỗi FORWARD trên iptables được đặt là \texttt{DROP}. Nghĩa là tất cả các gói tin đi xuyên qua Firewall đều bị hủy bỏ trừ khi có một quy tắc (rule) cho phép tường minh.

\subsection{Cấu hình Extended ACL (Lọc dịch vụ)}
Extended ACL cho phép kiểm soát luồng dữ liệu một cách tinh xảo dựa trên IP nguồn, IP đích, giao thức (TCP/UDP) và số hiệu cổng (Port). Trong mô hình này, vùng DMZ chứa Web Server, do đó các máy trạm từ Inside chỉ được phép truy cập vào DMZ qua Port 80 (HTTP) và Port 443 (HTTPS).
\begin{lstlisting}[style=powershell]
# Cho phep cac ket noi tra ve hop le
iptables -A FORWARD -m state --state ESTABLISHED,RELATED -j ACCEPT
# Extended ACL mo port 80, 443 vao DMZ
iptables -A FORWARD -s 192.168.10.0/24 -d 10.10.10.0/24 -p tcp --dport 80 -j ACCEPT
\end{lstlisting}

\subsection{Cấu hình Standard ACL (Lọc nguồn)}
Để tăng cường bảo mật cho máy chủ, hệ thống thiết lập một Standard ACL nhằm ngăn chặn hoàn toàn việc truy cập dịch vụ quản trị từ xa (SSH - Port 22) từ dải mạng \texttt{192.168.20.0/24} (giả sử đây là dải mạng của khách vãng lai hoặc thiết bị IoT).
\begin{lstlisting}[style=powershell]
iptables -A FORWARD -s 192.168.20.0/24 -d 10.10.10.0/24 -p tcp --dport 22 -j DROP
\end{lstlisting}

Ngoài ra, một nguyên tắc bất di bất dịch của DMZ là: Server trong DMZ không bao giờ được phép khởi tạo kết nối (Initiate Connection) ngược trở lại vùng Inside mạng nội bộ để đề phòng trường hợp Server bị Hacker chiếm quyền điều khiển:
\begin{lstlisting}[style=powershell]
iptables -A FORWARD -s 10.10.10.0/24 -d 192.168.0.0/16 -j DROP
\end{lstlisting}

\subsection{Tối ưu hóa kiến trúc ACL và Trực quan hóa (Mức 3)}
\subsubsection{Chiến lược đặt ACL tối ưu}
Hệ thống tuân thủ nguyên tắc tối ưu hóa băng thông (Cost-efficiency):
\begin{itemize}
    \item \textbf{Extended ACL (Đặt gần nguồn):} Các quy tắc lọc cổng dịch vụ (80/443) được đặt tại Firewall gần phía người dùng Inside nhất có thể. Mục tiêu là loại bỏ các gói tin rác (như SSH trái phép) ngay lập tức, tránh việc các gói tin này chiếm dụng băng thông của lớp Core và Distribution.
    \item \textbf{Standard ACL (Đặt gần đích):} Các quy tắc chặn theo IP nguồn đơn thuần được đặt tại điểm cuối cùng trước khi vào vùng nhạy cảm để đảm bảo tính bao quát và đơn giản trong quản lý.
\end{itemize}

\subsubsection{Trực quan hóa mức cao: Biểu đồ nhiệt (Security Heatmap)}
Để chứng minh hiệu quả của hệ thống phòng thủ, một kịch bản giả lập tấn công (Port Scan/Ping Sweep) đã được thực hiện vào DMZ. Toàn bộ các gói tin bị ACL chặn (Drop) đều được Firewall ghi nhật ký (Syslog).

\begin{figure}[H]
    \centering
    % [HUONG DAN]: Chạy script scripts/security_heatmap.py. 
    % Ảnh sinh ra tại results/heatmap_acl.png. Copy vào image/heatmap_acl.png.
    % \includegraphics[width=0.9\textwidth]{image/heatmap_acl.png}
    \caption{Security Heatmap: Trực quan hóa các nỗ lực tấn công bị ACL chặn theo thời gian}
    \label{fig:heatmap}
\end{figure}
\textit{Giải thích: Các điểm màu đỏ đậm trên Heatmap (Hình \ref{fig:heatmap}) thể hiện cường độ tấn công cao từ một IP nguồn nhất định tại một khung giờ cụ thể. Điều này giúp quản trị viên nhanh chóng nhận diện và phản ứng với các cuộc tấn công DDoS hoặc dò quét dịch vụ.}
